- 采用注册表模式（Registry Pattern），通过 `registry.py` 集中管理工具元数据与实现映射，解耦工具定义与调用逻辑。
- 内部划分为数据统计（`stats.py`）、数据访问（`data_access.py`）和网络检索（`web_data.py`）三个功能子域，由注册表统一对外暴露接口。
- 调度器 `dispatch` 根据工具类型自动处理数据集加载上下文，实现统计工具与原始数据的透明绑定。
- 所有工具函数遵循统一的字典返回契约，确保上层调用者（如 Agent）能一致地处理成功结果与错误状态。