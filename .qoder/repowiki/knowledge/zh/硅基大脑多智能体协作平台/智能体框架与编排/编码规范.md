- 所有 Agent 的思考产出均定义为结构化数据类（如 `ThinkingResult`），由调用方负责持久化与事件发布，实现逻辑与存储解耦。
- LLM 交互统一采用 System Prompt 注入角色视角与性格向量，User Prompt 聚焦具体上下文，并要求输出严格符合 Schema 的 JSON。
- 角色配置采用“静态定义 + 数据库持久化”的双层缓存机制，`RoleRegistry` 优先读取内存缓存，未命中时回查数据库并自动补全默认值。