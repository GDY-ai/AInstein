- 所有模块通过 database.py 提供的上下文管理器 get_db() 访问 SQLite，确保事务安全与 WAL 模式启用。
- 智能体与研究引擎均通过 agents/llm_client.py 封装的 call_llm 及 extract_json 进行模型调用与结构化输出解析。
- 业务逻辑层（如 cognitive, deliberation）与表现层（app.py）分离，API 路由仅负责参数校验与结果序列化。